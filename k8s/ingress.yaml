apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hmp-ingress
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: leader
                port:
                  number: 80
